# وضعیت پروژه — گزارش صادقانه

آخرین کامیت: `bf55a7f` · شاخه: `arena/019fafa0-vinabot`

---

## ✅ بیلد ۱۳ — موفق، APK آپلود شد

| مورد | مقدار |
|------|-------|
| Workflow | [run 30555198746](https://github.com/amirabbasbakhtiary282-cmd/vinabot/actions/runs/30555198746) |
| فایل | `vinabot-1.0.0-arm64-v8a-debug.apk` |
| حجم | 28,107,659 بایت (~۲۷ مگابایت) |
| SHA256 | `f9cd60117b58f0217dfa89d111a9524998e5f97cdd0ee068d37efa7a81b87d73` |
| Artifact | `vinabot-apk` (id 8764566811) — قابل دانلود |

کتابخانه‌های بومی داخل APK: `libllama.so`، `libvina_llm.so`،
`libggml-base/cpu/.so`، `libc++_shared.so`، `libvosk.so`،
`libpython3.14.so`، `libsqlite3.so`.

---

## 🔧 کرش هنگام باز شدن — سه علت پیدا و رفع شد

### ۱) بلاک شدن ترد اصلی (علت اصلی)

`VinaVoice.__init__` داخل `VinaApp.build()` روی **ترد رابط کاربری**
اجرا می‌شود و `set_tts_language_fa()` را صدا می‌زد. آن تابع از طریق
`_ensure_tts()` این کار را می‌کرد:

```python
if not ready.wait(timeout=5.0):
```

یعنی ترد اصلی تا **۵ ثانیه** قفل می‌شد. اندروید برنامه‌ای را که هنگام
راه‌اندازی ترد اصلی را بلاک کند می‌کشد — دقیقاً منطبق با گزارش شما:
«Loading برای حدود یک ثانیه، سپس بسته شدن».

**رفع:** `set_tts_language_fa_async()` که مقداردهی TTS را روی ترد
پس‌زمینه انجام می‌دهد. ترد اصلی هرگز بلاک نمی‌شود.

### ۲) جدا نشدن تردهای pyjnius از JVM

مستندات رسمی pyjnius صریح است: هر ترد بومی که JNI را لمس کند باید قبل
از خروج `detach()` شود، وگرنه:

```
Native thread exited without calling DetachCurrentThread
Runtime aborting...
```

ترد بارگذاری مدل از طریق `model_paths` (فراخوانی `getExternalFilesDir`)
با JNI کار می‌کند و هرگز detach نمی‌شد.

**رفع:** کمکی `_detach_jnius()` و فراخوانی آن در `finally` تردها.

### ۳) نبود شبکه‌ی ایمنی

هر استثنایی در `build()` برنامه را **بی‌صدا** می‌بست.

**رفع:** کل `build()` داخل `try/except`. حالا در صورت خطا:
- یک صفحه‌ی خطای خوانا نمایش داده می‌شود (فقط با ویجت‌های ابتدایی، تا
  حتی اگر تم/design_system خراب باشد کار کند)
- متن کامل traceback در `vina_crash_log.txt` داخل پوشه‌ی مدل‌ها ذخیره
  می‌شود — جایی که با فایل‌منیجر قابل دسترسی است، بدون نیاز به ADB

---

## 📁 بدون نیاز به ساخت دستی پوشه

- `ensure_models_dir()` حالا **همه‌ی** مسیرهای کاندید را می‌سازد، نه فقط
  اولین مسیر قابل نوشتن.
- جستجوی **بازگشتی تا دو سطح**: مدل داخل زیرپوشه هم پیدا می‌شود.
- فایل‌های خالی و نیمه‌کپی‌شده (`.importing`/`.part`) نادیده گرفته
  می‌شوند تا خطای مبهم ندهند.

## 🛡️ درباره‌ی احتمال ناسازگاری کتابخانه‌های بومی

بررسی شد و **به‌عنوان علت کرش رد شد**، اما مقاوم‌سازی انجام شد:

- `GGML_NATIVE=OFF` در recipe یعنی llama.cpp برای **armv8-a پایه**
  کامپایل می‌شود، نه با `-mcpu=native`. هیچ دستور CPU خاصی که روی
  دستگاه‌های قدیمی‌تر SIGILL بدهد استفاده نمی‌شود.
- کتابخانه‌های بومی **تنبل** بارگذاری می‌شوند (فقط هنگام بارگذاری مدل)،
  پس نمی‌توانستند باعث کرش هنگام راه‌اندازی شوند.
- با این حال `_load_native_lib` و `vina_llm_load` داخل
  `try/except BaseException` قرار گرفتند تا هر ناسازگاری احتمالی فقط
  قابلیت هوش مصنوعی را غیرفعال کند، نه کل برنامه را.

---

## ✅ تأییدشده

- APK ساخته و آپلود می‌شود (۲۸ مگابایت، همه‌ی کتابخانه‌ها)
- ۱۵۷ تست واحد سبز (شامل ۱۲ تست جدید ایمنی راه‌اندازی)
- تست‌های رگرسیون **واقعاً** باگ‌ها را می‌گیرند (با برگرداندن هر باگ
  بررسی شد که تست قرمز می‌شود)
- تشخیص مدل: زیرپوشه، پسوند بزرگ/کوچک، فایل ناقص

## ❌ تأییدنشده (نیازمند دستگاه فیزیکی)

- باز شدن واقعی برنامه روی گوشی شما
- پاسخ دادن مدل GGUF روی سخت‌افزار موبایل
- کیفیت TTS/STT فارسی

اگر با APK بیلد ۱۳ همچنان مشکلی بود، فایل
`vina_crash_log.txt` در پوشه‌ی مدل‌ها متن کامل خطا را خواهد داشت.
