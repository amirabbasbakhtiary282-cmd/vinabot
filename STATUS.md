# وضعیت پروژه — گزارش صادقانه

> **این گزارش «تکمیل‌شده» اعلام نمی‌کند.** شرط‌های توقفی که تعیین کردید
> هنوز برآورده نشده‌اند، چون بیلد واقعی APK هنوز اجرا نشده است. دلیلش
> در بخش «مسدودکننده» آمده.

آخرین کامیت: `c061834` · شاخه: `arena/019fafa0-vinabot`

---

## 🚧 مسدودکننده — یک کار از شما لازم است

من نمی‌توانم فایل workflow را تغییر دهم یا بیلد را اجرا کنم. **هر دو**
راه تست شد و هر دو ۴۰۳ دادند:

```
git push  → refusing to allow a GitHub App to create or update workflow
            `.github/workflows/build.yml` without `workflows` permission
gh workflow run → HTTP 403: Resource not accessible by integration
```

workflow فعلی هم فقط روی `main`/`master` اجرا می‌شود، پس روی این شاخه
هرگز اجرا نمی‌شود.

**سه دستور لازم:**

```bash
cp ci/build-apk.proposed.yml .github/workflows/build.yml
git add .github/workflows/build.yml
git commit -m "CI: fail on real build errors, verify APK"
git push origin arena/019fafa0-vinabot
```

بعد از این، بیلد خودکار شروع می‌شود و من می‌توانم لاگ را بخوانم و
تا رسیدن به APK واقعی ادامه دهم.

---

## ✅ چه چیزی واقعاً تأیید شد

### باگ اصلی که بیلد را می‌شکست — پیدا و رفع شد

فرضیه‌ی عامل قبلی («tee و artifact») **درست ولی ناقص** بود. آن دو باعث
*پنهان شدن* شکست می‌شدند، اما علت خودِ شکست چیز دیگری بود. از لاگ خام
اجرای شماره ۷:

```
File "p4a-recipes/vinallm/__init__.py", line 83, in build_arch
RAN: /usr/bin/ccache -std=c++17 -shared -fPIC -O2 ...
STDOUT: /usr/bin/ccache: invalid option -- 't'
```

recipe کامپایلر را با `env['CC'].split()[0]` انتخاب می‌کرد، اما p4a
مقدار `CC` را `"{ccache} {clang} {cflags}"` می‌سازد → **ccache به‌عنوان
کامپایلر اجرا می‌شد**.

این باگ روی ماشین بدون ccache اصلاً ظاهر نمی‌شود؛ برای همین قبلاً پیدا
نشده بود.

**تأیید رفع** (شبیه‌سازی با ccache در محیط):
```
clang++ -target aarch64-linux-android24 -march=armv8-a -fPIC -std=c++17 ...
نتیجه: هیچ فراخوانی ccache نیست
```

### دو باگ کرش‌کننده‌ی دیگر که خودم پیدا کردم

| باگ | اثر | تأیید |
|-----|-----|-------|
| `main.py` نام `SETTINGS_SECTIONS` را از ماژول اشتباه import می‌کرد | `ImportError` — برنامه **قبل از نمایش هر چیزی** کرش می‌کرد | با import واقعی بازتولید و رفع شد |
| مسیر دیتابیس اول پوشه‌ی فقط-خواندنی APK را امتحان می‌کرد | `unable to open database file` — کل حافظه از کار می‌افتاد | با پوشه‌ی ۰۵۵۵ بازتولید شد |

### تأییدهای عملی (نه حدس)

- **لایه‌ی ctypes روی `.so` واقعاً کامپایل‌شده**: بارگذاری، تولید
  جریانی توکن، متن فارسی UTF-8، و لغو وسط تولید (barge-in) — همه اجرا
  و تأیید شدند.
- **هر ۱۲ سمبل C** با `-fvisibility=hidden` هنوز export می‌شوند (`nm -D`).
- **`vina_llm.cpp` با هدرهای واقعی llama.cpp b5026 کامپایل می‌شود** بدون خطا.
- **مدل پیش‌فرض `gemma-3-1b-it-Q4_K_M.gguf` بدون هیچ تغییر کد شناسایی و
  بارگذاری می‌شود.**
- **حالت بدون مدل**: پوشه ساخته می‌شود، پیام راهنما با مسیر واقعی نشان
  داده می‌شود، برنامه کرش نمی‌کند.
- **۱۴۳ تست** سبز (۱۳۶ بدون Kivy + ۷ تست UI با Kivy).
- **`pipefail`**: اثبات شد که فرم قبلی روی شکست کد ۰ برمی‌گرداند و فرم
  جدید ۱.

---

## ❌ چه چیزی تأیید **نشده**

صادقانه، هیچ‌کدام از این‌ها را نمی‌توانم ادعا کنم:

- ❌ ساخته شدن APK (بیلد اجرا نشده)
- ❌ نصب شدن APK روی دستگاه
- ❌ باز شدن برنامه روی گوشی واقعی
- ❌ پاسخ دادن مدل GGUF روی سخت‌افزار موبایل
- ❌ کیفیت واقعی TTS/STT فارسی
- ❌ ظاهر و رندر واقعی رابط کاربری (سندباکس OpenGL ندارد)
- ❌ در دسترس بودن `libvosk.so` (میزبان دانلود از سندباکس مسدود است)

---

## چرا برخی چیزها را نمی‌توانستم تست کنم

شبکه‌ی سندباکس محدود است. فقط `github.com` و `pypi.org` در دسترس‌اند:

| میزبان | وضعیت | پیامد |
|--------|-------|-------|
| `dl.google.com` | ❌ | نصب Android SDK/NDK ممکن نیست |
| `repo.maven.apache.org` | ❌ | تأیید ساختار aar وسک ممکن نیست |
| `*.blob.core.windows.net` | ❌ | دانلود artifact با `gh` کار نمی‌کند |
| مخازن Debian | ❌ | نصب OpenGL برای رندر Kivy ممکن نیست |

به همین دلیل بیلد واقعی **باید** در GitHub Actions انجام شود.

---

## مراحل بعدی (بعد از اعمال workflow)

1. بیلد اجرا می‌شود؛ من لاگ را می‌خوانم.
2. هر خطای واقعی را رفع می‌کنم و دوباره push می‌کنم.
3. تکرار تا وقتی APK واقعاً تولید شود.
4. گزارش نهایی با هش کامیت، شماره‌ی بیلد، لینک workflow و SHA256 فایل APK.

نصب روی گوشی، اجرای برنامه و پاسخ‌گویی مدل را **شما** باید تأیید کنید؛
من به دستگاه فیزیکی دسترسی ندارم و آن‌ها را بدون شواهد «تأییدشده»
اعلام نخواهم کرد.
