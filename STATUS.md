# وضعیت پروژه — گزارش صادقانه

آخرین کامیت: `c6c16ab` · شاخه: `arena/019fafa0-vinabot`

---

## ✅ خبر مهم: APK واقعاً ساخته می‌شود

بیلد شماره ۱۰ (`30544748972`) یک APK کامل و سالم تولید کرد:

```
bin/vinabot-1.0.0-arm64-v8a-debug.apk      28,235,707 بایت (~۲۸ مگابایت)
```

کتابخانه‌های native داخل APK (از لاگ واقعی بیلد):

| کتابخانه | حجم | معنی |
|----------|-----|------|
| `libllama.so` | 1,426,880 | موتور llama.cpp ✅ |
| `libvina_llm.so` | 41,464 | پل C اختصاصی وینا ✅ |
| `libggml-base.so` | 624,992 | هسته‌ی ggml ✅ |
| `libggml-cpu.so` | 427,376 | بک‌اند CPU ✅ |
| `libggml.so` | 56,224 | ggml ✅ |
| `libc++_shared.so` | 1,253,544 | STL (بدون این، dlopen کرش می‌کند) ✅ |
| `libvosk.so` | 8,863,712 | تشخیص گفتار آفلاین ✅ |
| `libpython3.14.so` | 5,768,296 | پایتون ✅ |
| `libsqlite3.so` | 1,237,256 | حافظه ✅ |

مرحله‌ی `Build APK` وضعیت **success** داشت و Gradle تسک‌های
`:packageDebug` و `:assembleDebug` را کامل کرد.

---

## 🚧 تنها مانع باقی‌مانده (یک خط)

بیلد ۱۰ فقط به این دلیل قرمز شد که **مرحله‌ی بررسی خودم** اشتباه بود:

```
##[error]کتابخانه‌ی ضروری در APK نیست: libpython3.11.so
```

نسخه‌ی پایتون را python-for-android انتخاب می‌کند (اینجا **3.14**)، نه
ما. هاردکد کردن `3.11` باعث شد یک APK کاملاً سالم رد شود. چون
`Upload APK` فقط در صورت موفقیت اجرا می‌شود، APK آپلود نشد.

این اشتباه در `ci/build-apk.proposed.yml` **اصلاح شده** (بررسی با الگوی
`libpython3.[0-9]+.so`)، اما من اجازه‌ی نوشتن روی
`.github/workflows/` را ندارم (۴۰۳ برای هر دو راه: git push و API).

### دستور لازم از شما

```bash
cp ci/build-apk.proposed.yml .github/workflows/build.yml
git add .github/workflows/build.yml
git commit -m "CI: fix hardcoded python lib version in APK verification"
git push origin arena/019fafa0-vinabot
```

بعد از این، بیلد ۱۱ باید سبز شود و APK به‌عنوان artifact آپلود گردد.

> منطق جدید روی محتویات **واقعی** APK بیلد ۹/۱۰ تست شد: قبول می‌شود، و
> با حذف `libllama.so` همچنان درست رد می‌کند.

---

## تاریخچه‌ی رفع باگ‌ها

| بیلد | نتیجه | علت واقعی |
|------|-------|-----------|
| #7 | ❌ (ولی «سبز» نشان داده شد) | `ccache` به‌عنوان کامپایلر صدا زده می‌شد |
| #8 | ❌ | `android.gradle_dependencies =` خالی → `--depend ''` |
| #9 | ❌ | APK ساخته شد؛ بررسی من `libpython3.11` می‌خواست |
| #10 | ❌ | همان مورد ۹ (workflow هنوز به‌روز نشده) |
| #11 | ⏳ | منتظر اعمال workflow |

### باگ بیلد ۸ — توضیح دقیق

```ini
android.gradle_dependencies =        # خالی، نه غایب
```

`SpecParser.getlist` به‌جای `[]` مقدار `['']` برمی‌گرداند، چون
`''.split(',') == ['']`. سپس buildozer آرگومان `--depend ''` می‌سازد و
قالب Gradle آن را به `implementation ''` تبدیل می‌کند:

```
Supplied String module notation '' is invalid.
```

با buildozer واقعی تأیید شد: قبل `['']` → بعد `[]`.

### چرا SyntaxErrorها مقصر نبودند

این‌ها فایل‌های **عمداً خراب** در مجموعه‌تست خود CPython هستند
(`uft-8`، BOM، بایت غیر UTF-8) که برای آزمودن رد شدن انکودینگ نامعتبر
ساخته شده‌اند. p4a خروجی `compileall` را با `subprocess.call` بدون
بررسی کد بازگشتی صدا می‌زند
(`recipes/python3/__init__.py` خط ۴۰۶)، پس **نمی‌توانند** بیلد را
متوقف کنند. اثبات نهایی: بیلد ۹ و ۱۰ با وجود همین SyntaxErrorها APK
سالم ساختند. حذف `Lib/test/*` هم بی‌فایده بود و هم ریسک شکستن stdlib
داشت.

---

## ❌ چه چیزی هنوز تأیید نشده

- ❌ نصب APK روی دستگاه واقعی
- ❌ باز شدن برنامه روی گوشی
- ❌ پاسخ دادن مدل GGUF روی سخت‌افزار موبایل
- ❌ کیفیت واقعی TTS/STT فارسی
- ❌ ظاهر و رندر واقعی رابط کاربری روی گوشی

این‌ها بدون دستگاه فیزیکی قابل تأیید نیستند و من آن‌ها را «تأییدشده»
اعلام نمی‌کنم.

## ✅ چه چیزی تأیید شده

- ✅ APK ساخته می‌شود (۲۸ مگابایت، همه‌ی کتابخانه‌های حیاتی)
- ✅ ۱۴۵ تست واحد سبز (در CI با OpenGL واقعی زیر xvfb)
- ✅ لایه‌ی ctypes روی `.so` واقعاً کامپایل‌شده: بارگذاری، جریان توکن،
  متن فارسی، و لغو وسط تولید (barge-in)
- ✅ مدل `gemma-3-1b-it-Q4_K_M.gguf` بدون تغییر کد شناسایی می‌شود
- ✅ حالت بدون مدل: پوشه ساخته می‌شود، پیام راهنما، بدون کرش
- ✅ سه باگ کرش‌کننده رفع شد (ccache، ImportError هنگام اجرا، مسیر
  فقط-خواندنی دیتابیس)
