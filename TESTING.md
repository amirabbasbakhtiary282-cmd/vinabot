# راهنمای تست وینا روی دستگاه واقعی — فاز A (گفتگوی صوتی)

> **مهم:** هر چیزی که در این فایل با ✅ علامت خورده، با تست خودکار روی
> منطق برنامه راستی‌آزمایی شده است. هر چیزی که با 🔬 علامت خورده، **فقط
> روی گوشی واقعی قابل تأیید است** و من نتوانسته‌ام آن را تست کنم.

---

## ۱. گرفتن APK

بیلد به‌صورت خودکار روی هر push به این شاخه اجرا می‌شود:

```
Actions → Build APK → آخرین اجرا → بخش Artifacts → vinabot-apk
```

فایل `build.log` هم در همان artifact هست؛ اگر بیلد شکست خورد، **همان فایل
را برای من بفرستید** (نه فقط اسکرین‌شات).

نصب روی گوشی:

```bash
adb install -r vinabot-1.0.0-arm64-v8a-debug.apk
```

---

## ۲. آماده‌سازی قبل از تست

### الف) گرفتن لاگ زنده (مهم‌ترین ابزار عیب‌یابی)

قبل از باز کردن برنامه، این را در ترمینال اجرا کنید و باز نگه دارید:

```bash
adb logcat -c                      # پاک کردن لاگ‌های قبلی
adb logcat | grep -iE "python|vina|AndroidRuntime|VoskAPI|SpeechRecog|AudioRecord|TextToSpeech"
```

برای ذخیره در فایل (برای فرستادن به من):

```bash
adb logcat -c && adb logcat > vina-log.txt
# ... برنامه را باز کنید و خطا را بازتولید کنید، سپس Ctrl+C
```

### ب) مدل زبانی (LLM)

بدون مدل، وینا فقط پاسخ‌های آماده می‌دهد. یک مدل GGUF بگذارید:

```bash
adb push qwen2.5-1.5b-instruct-q4_k_m.gguf /sdcard/Android/data/org.vinabot/files/models/
```

### ج) مدل تشخیص گفتار آفلاین (Vosk)

از داخل برنامه: **تنظیمات ← صدا ← مدل‌های آفلاین ← فارسی ← دانلود**
(حدود ۴۷ مگابایت، یک‌بار برای همیشه). بعد از این، تشخیص گفتار کاملاً
آفلاین است.

---

## ۳. چک‌لیست تست گفتگوی صوتی

برای هر مورد: نتیجه را ✅/❌ بزنید و در صورت ❌ لاگ را بفرستید.

### ۳-۱ مجوز میکروفون
| # | مرحله | انتظار |
|---|-------|--------|
| 1 | صفحه‌ی «حالت صوتی» را باز کنید و دکمه‌ی میکروفون را بزنید | پنجره‌ی درخواست مجوز ضبط صدا ظاهر شود |
| 2 | «رد کردن» را بزنید | پیام فارسی «اجازه‌ی دسترسی به میکروفون لازم است» — **نباید کرش کند** |
| 3 | دوباره بزنید و «اجازه دادن» | وضعیت به «گوش می‌دهم... صحبت کنید» تغییر کند |

### ۳-۲ مسیر کامل صدا 🔬
| # | مرحله | انتظار |
|---|-------|--------|
| 4 | بگویید: «سلام حالت چطوره؟» | متن گفته‌ی شما زنده زیر انیمیشن ظاهر شود |
| 5 | ساکت شوید | حدود ۰.۸ ثانیه بعد، وضعیت به «در حال فکر کردن» برود |
| 6 | صبر کنید | وینا شروع به **حرف زدن** کند |
| 7 | دقت کنید به زمان‌بندی | وینا باید **جمله‌ی اول را قبل از تمام‌شدن کل پاسخ** بگوید (نه اینکه چند ثانیه سکوت و بعد همه را یک‌جا) |
| 8 | بعد از پایان پاسخ | خودش دوباره به حالت «گوش می‌دهم» برگردد (بدون زدن دکمه) |
| 9 | یک سؤال دیگر بپرسید | نوبت دوم هم کار کند |

### ۳-۳ قطع کردن وسط صحبت (barge-in) 🔬
| # | مرحله | انتظار |
|---|-------|--------|
| 10 | سؤالی بپرسید که پاسخ بلند دارد («درباره‌ی تاریخ ایران توضیح بده») | وینا شروع به حرف زدن کند |
| 11 | **وسط حرفش** بلند بگویید «صبر کن» | باید **فوراً ساکت شود** و به حالت گوش دادن برگردد |
| 12 | بلافاصله سؤال جدید بپرسید | باید به سؤال جدید پاسخ دهد |
| 13 | تست منفی: وقتی وینا حرف می‌زند **ساکت بمانید** | نباید خودبه‌خود قطع شود (صدای بلندگو نباید باعث قطع شود) |

اگر مورد ۱۳ شکست خورد (وینا خودش را قطع می‌کند)، این را بفرستید:
`adb logcat | grep -i "barge\|speech_start"` — احتمالاً باید مقدار
`BARGE_IN_SENSITIVITY` در `src/voice_conversation.py` بالاتر برود.

### ۳-۴ آفلاین بودن (حریم خصوصی) 🔬
| # | مرحله | انتظار |
|---|-------|--------|
| 14 | مدل Vosk را دانلود کنید، سپس **حالت پرواز** را روشن کنید | |
| 15 | گفتگوی صوتی را امتحان کنید | باید **کامل کار کند** (STT + LLM + TTS) بدون اینترنت |
| 16 | تنظیمات ← صدا | باید بنویسد «موتور فعال: آفلاین (Vosk)» با رنگ سبز |

### ۳-۵ تعویض موتور STT
| # | مرحله | انتظار |
|---|-------|--------|
| 17 | تنظیمات ← صدا ← «تشخیص گفتار آفلاین» را خاموش کنید | پیام «آنلاین (گوگل)» با رنگ نارنجی |
| 18 | گفتگو کنید | با موتور گوگل کار کند (نیاز به اینترنت) |
| 19 | دوباره روشن کنید | برگردد به آفلاین |

### ۳-۶ کلمه‌ی فعال‌سازی
| # | مرحله | انتظار |
|---|-------|--------|
| 20 | تنظیمات ← صدا ← «هی وینا» را روشن کنید | |
| 21 | بگویید «امروز هوا خوبه» | **نباید** پاسخ دهد |
| 22 | بگویید «هی وینا ساعت چنده» | باید پاسخ دهد |

### ۳-۷ پایداری 🔬
| # | مرحله | انتظار |
|---|-------|--------|
| 23 | ۱۰ نوبت پشت سر هم گفتگو کنید | کرش نکند، کند نشود |
| 24 | وسط گفتگو دکمه‌ی میکروفون را بزنید | گفتگو تمیز تمام شود |
| 25 | به صفحه‌ی دیگر بروید و برگردید | حالت درست نمایش داده شود |
| 26 | مصرف RAM را ببینید: `adb shell dumpsys meminfo org.vinabot` | با مدل ۱.۵B زیر ~۲GB بماند |

---

## ۴. آنچه در این فاز **کار نمی‌کند** (صادقانه)

اینها هنوز ساخته نشده‌اند و در فازهای بعدی می‌آیند:

- ❌ کنترل دستگاه (وای‌فای، بلوتوث، چراغ‌قوه، تماس، پیامک...) — **فاز B**
- ❌ سرویس پیش‌زمینه / ادامه‌ی کار با صفحه‌ی خاموش — **فاز D**
- ❌ خواندن نوتیفیکیشن‌ها — **فاز D** (نیازمند سرویس جاوا)
- ❌ اجرای خودکار بعد از روشن شدن گوشی — **فاز D**
- ❌ جستجو و خروجی گرفتن از تاریخچه — **فاز D**

محدودیت‌های دائمی سیستم‌عامل (هرگز ممکن نیست بدون روت):

- خاموش/ری‌استارت کردن گوشی
- باز کردن قفل صفحه
- تغییر مستقیم وای‌فای/بلوتوث در اندروید ۱۰+ (فقط باز کردن پنل تنظیمات)
- اسکرین‌شات از کل صفحه (فقط از پنجره‌ی خود برنامه)

---

## ۵. گزارش خطا

لطفاً برای هر خطا این سه چیز را بفرستید:

1. **شماره‌ی مورد** از چک‌لیست بالا (مثلاً «مورد ۱۱ شکست خورد»)
2. **چه دیدید** در مقابل چه انتظار داشتید
3. **لاگ**:
   ```bash
   adb logcat -d > vina-log.txt
   ```
   و اگر کرش بود، این بخش مهم‌تر است:
   ```bash
   adb logcat -d | grep -A 40 "AndroidRuntime\|Traceback"
   ```

---

## ۶. تست‌های خودکاری که همین حالا پاس می‌شوند

روی هر push در CI اجرا می‌شوند (`python -m unittest discover -s tests`):

| فایل | تعداد | چه چیزی را تضمین می‌کند |
|------|-------|--------------------------|
| `test_speech_pipeline.py` | ۲۲ | تکه‌کردن جمله (فارسی/انگلیسی)، VAD، تطبیق کلمه‌ی فعال‌سازی |
| `test_voice_conversation.py` | ۱۷ | مسیر کامل صدا، **قطع کردن وسط صحبت**، چند نوبت، مدیریت خطا |
| `test_stt_engine.py` | ۱۲ | انتخاب موتور، بازگشت امن، استخراج امن zip (Zip Slip) |
| `test_brain_streaming.py` | ۸ | تولید توکن‌به‌توکن، توقف تولید، سازگاری با کد قدیمی |
| `test_app_wiring.py` | ۱۱ | هر دکمه/تنظیم به متد واقعاً موجود وصل است |
| **مجموع** | **۷۰** | |

نکته: این‌ها منطق را تضمین می‌کنند، **نه** رفتار سخت‌افزار واقعی. برای
میکروفون، بلندگو و موتور TTS گوشی، چک‌لیست بخش ۳ لازم است.
