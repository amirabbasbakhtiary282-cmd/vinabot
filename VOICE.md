# قابلیت‌های صوتی وینا — چه چیزی کار می‌کند و چه چیزی نه

این سند عمداً صریح است. هر جا محدودیت فنی واقعی وجود دارد، به‌جای
وانمود کردن به کارکرد، دلیلش توضیح داده شده.

---

## ۱. متن به گفتار (TTS)

**پیاده‌سازی:** `android.speech.tts.TextToSpeech` از طریق pyjnius
(`src/android_bridge.py`).

| ویژگی | وضعیت |
|-------|-------|
| گفتن متن فارسی | ✅ پیاده‌سازی شده |
| تنظیم سرعت گفتار | ✅ `set_speech_rate` |
| تنظیم زیر و بمی | ✅ `set_pitch` |
| صف کردن جمله‌به‌جمله (گفتار جریانی) | ✅ `speak_queued` |
| قطع فوری | ✅ `stop_speaking` |

### محدودیت واقعی

TTS فارسی **وابسته به گوشی است**. اندروید موتور TTS فارسی را به‌صورت
پیش‌فرض روی همه‌ی دستگاه‌ها ندارد. اگر داده‌ی زبان فارسی نصب نباشد،
`setLanguage` مقدار `LANG_MISSING_DATA` یا `LANG_NOT_SUPPORTED`
برمی‌گرداند و صدایی پخش نمی‌شود.

**راه‌حل کاربر:** تنظیمات اندروید ← Language & input ← Text-to-speech
← نصب داده‌ی صوتی فارسی (معمولاً از طریق Google Speech Services).

این موضوع از کنترل برنامه خارج است؛ هیچ اپ اندرویدی نمی‌تواند موتور TTS
فارسی را خودش نصب کند.

---

## ۲. گفتار به متن (STT)

دو موتور قابل انتخاب (`src/stt_engine.py`):

### الف) Vosk — آفلاین (پیش‌فرض)

| مورد | جزئیات |
|------|--------|
| حریم خصوصی | ✅ هیچ بایتی از صدا از گوشی خارج نمی‌شود |
| اینترنت | ❌ لازم نیست (بعد از دانلود اولیه‌ی مدل) |
| مدل فارسی | ~۴۷ مگابایت، بار اول دانلود می‌شود |
| اتصال | ctypes مستقیم به `libvosk.so` (بدون cffi) |

**محدودیت:** دقت مدل کوچک فارسی از سرویس آنلاین گوگل کمتر است. این یک
مصالحه‌ی آگاهانه به نفع حریم خصوصی است.

> ⚠️ **وضعیت تأیید:** `libvosk.so` از artifact رسمی Maven گرفته می‌شود.
> اگر دانلود در زمان بیلد شکست بخورد، **بیلد متوقف نمی‌شود** و برنامه
> به‌صورت خودکار به موتور اندروید برمی‌گردد. برای فهمیدن اینکه STT
> آفلاین در APK شما فعال است یا نه، به خلاصه‌ی بیلد نگاه کنید: اگر
> `libvosk.so` در APK نباشد، یک `::warning` ثبت می‌شود.

### ب) SpeechRecognizer اندروید — اختیاری

دقت بالاتر، اما **روی اکثر گوشی‌ها صدا به سرور گوگل فرستاده می‌شود**.
به همین دلیل پیش‌فرض نیست و کاربر باید صریحاً از تنظیمات فعالش کند.

---

## ۳. حالت‌های تعامل صوتی

| حالت | وضعیت | توضیح |
|------|-------|-------|
| Push-to-talk | ✅ | نگه داشتن دکمه‌ی میکروفون |
| شنیدن پیوسته | ✅ | VAD مبتنی بر انرژی، جدا کردن خودکار گفتار |
| گفتگوی زنده‌ی دوطرفه | ✅ | `src/voice_conversation.py` |
| قطع کردن وسط صحبت (barge-in) | ✅ | تست‌شده روی کتابخانه‌ی native واقعی |
| کلمه‌ی فعال‌سازی (wake word) | ⚠️ محدود | توضیح پایین |

### چرا barge-in واقعاً کار می‌کند

وقتی وینا در حال صحبت است و کاربر شروع به حرف زدن می‌کند، VAD آن را
تشخیص می‌دهد، TTS فوراً قطع می‌شود و تولید مدل هم لغو می‌گردد. برای
جلوگیری از قطع کاذب توسط *صدای خود بلندگو*:

- یک بازه‌ی محافظ ۰.۶ ثانیه‌ای بعد از شروع گفتن
- آستانه‌ی انرژی ۲ برابر در حالت صحبت کردن

### کلمه‌ی فعال‌سازی — محدودیت صادقانه

**آنچه پیاده‌سازی شده:** VAD یک قطعه‌ی گفتار را جدا می‌کند → STT آفلاین
آن را به متن تبدیل می‌کند → متن با عبارت‌های فعال‌سازی مقایسه می‌شود
(«هی وینا»، «سلام وینا»، …).

**آنچه پیاده‌سازی *نشده*:** keyword spotting سخت‌افزاری/همیشه‌روشن مثل
Porcupine یا «Hey Google».

**چرا:**

1. Porcupine برای استفاده‌ی تجاری کلید فعال‌سازی آنلاین می‌خواهد.
2. wake word واقعیِ همیشه‌روشن نیازمند سرویس پیش‌زمینه‌ی دائمی است که
   باتری را به‌شدت مصرف می‌کند و در اندروید ۸+ با محدودیت‌های اجرای
   پس‌زمینه برخورد می‌کند.
3. روش فعلی فقط وقتی برنامه باز و در حالت شنیدن است کار می‌کند — نه
   وقتی صفحه خاموش است یا کاربر در اپ دیگری است.

اگر انتظار «همیشه گوش‌به‌زنگ بودن مثل دستیار گوگل» دارید، این قابلیت
با معماری فعلی برآورده **نمی‌شود** و نباید غیر از این ادعا شود.

---

## ۴. مجوزها

| مجوز | نوع | زمان درخواست |
|------|-----|--------------|
| `RECORD_AUDIO` | dangerous | زمان اجرا (الزامی) |
| `POST_NOTIFICATIONS` | dangerous (اندروید ۱۳+) | زمان اجرا |
| `INTERNET` | normal | زمان نصب — درخواست runtime بی‌اثر است |
| `FOREGROUND_SERVICE_MICROPHONE` | ویژه | اندروید ۱۴+ |

اگر کاربر `RECORD_AUDIO` را رد کند، وضعیت صوتی روی `no_permission`
می‌رود و در لاگ ثبت می‌شود — برنامه کرش نمی‌کند.

---

## ۵. آنچه در این محیط قابل تأیید نبود

صادقانه: موارد زیر **کد نوشته شده و از نظر منطقی تست شده‌اند**، اما
اجرای واقعی روی یک گوشی اندروید فیزیکی لازم دارند و در محیط CI/سندباکس
قابل تأیید نیستند:

- کیفیت و قابل‌فهم بودن صدای TTS فارسی
- دقت واقعی تشخیص گفتار فارسی
- رفتار barge-in با صدای واقعی بلندگو و میکروفون (نه داده‌ی شبیه‌سازی‌شده)
- تأخیر (latency) کل زنجیره روی سخت‌افزار موبایل
- مصرف باتری در حالت شنیدن پیوسته

منطق VAD، تکه‌کننده‌ی جمله، تطبیق کلمه‌ی فعال‌سازی و ماشین وضعیت گفتگو
با ۹۰+ تست واحد پوشش داده شده‌اند، و لایه‌ی ctypes (شامل جریان توکن و
لغو وسط تولید) روی یک کتابخانه‌ی `.so` واقعاً کامپایل‌شده تست شده است.
